import {describe} from '@jest/globals'
import 'reflect-metadata';
import {Provider} from '../../provider'
import {D} from '../abc/abcd';


describe('Injector', () => {

  test('check', () => {

    // const provider = Provider.of();
    // const result = provider.getAll(D);
    // expect(1).toBe(result);

  });

});
